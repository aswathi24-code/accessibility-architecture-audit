# Project Architecture

## Structure

- `client/` — React frontend
- `server/` — Express backend
- `docs/` — Project documentation
- `tests/` — Testing files

## Frontend

React + Vite is used to create the accessible user interface.

## Backend

Node.js + Express provides the API.

## Goal

Keep the project simple, modular, accessible, and easy to maintain.
